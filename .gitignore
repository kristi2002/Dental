node_modules/
.next/
out/
build/
dist/

# env
.env
.env.local
.env*.local

# prisma
/src/generated
*.db
*.db-journal

# Staged out of node_modules by scripts/copy-zxing-wasm.mjs on install and on
# build. Not committed, so it can never drift from the version of the decoder
# that loads it.
/public/zxing_reader.wasm

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
next-env.d.ts

# Patient files (X-rays, photos, consent forms) — real medical data, never in git
/storage

# Scratch work — sketches, screenshots, bundles pulled apart to look at.
# Never part of the app, and never committed.
/.scratch

# Playwright: the run's own artefacts, session cookie and upload directory.
/test-results/
/playwright-report/
/blob-report/
/e2e/.auth/
/e2e/.storage/
